@model IEnumerable<Mantle.Web.ContentManagement.Areas.Admin.ContentBlocks.IContentBlock>

@{
    Layout = null;
}

@foreach (var block in Model.OrderBy(x => x.Order))
{
    if (!string.IsNullOrEmpty(block.CustomTemplatePath))
    {
        @Html.Partial(block.CustomTemplatePath, block)
    }
    else
    {   // Html.Partial won't be able to find the view if the ".cshtml" extension is present
        @Html.Partial(block.DisplayTemplatePath.Replace(".cshtml", string.Empty), block)
    }
}